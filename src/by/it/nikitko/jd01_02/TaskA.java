package by.it.nikitko.jd01_02;


import java.util.Scanner;

public class TaskA {
    public static void main(String[] args) {/*
        java.util.Scanner scanner = new Scanner(System.in);
        int[] arraay = new int[10];
        for (int i = 0; i < arraay.length; i++) {
            arraay[i] = scanner.nextInt();

        }
        //  System.out.println(Arrays);
        step1(arraay);
Scanner scanner = new Scanner(System.in);
Scanner scanner = new Scanner(System.in);
    }

    private static void step1(int[] arraay) {
        int min = arraay[0];
        int max = arraay[0];
        for (int element : arraay) {
            if (min > element) {
                min = element;
            }
            if (max < element) {
                max = element;
            }
        }
        System.out.println(min + " " + max);
    }
}

    private static void step2(int[] array) {
        double sum = 0;
        for (int i)
        */
    }
    }